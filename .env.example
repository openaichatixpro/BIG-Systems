## Example environment variables for host-specific overrides
# Set this to a host folder on your I: drive (or another large drive)
HOST_OLLAMA_MODELS_PATH=I:/Ollama_Archive

# Optional: path to store project data (used by helper migration script)
HOST_PROJECT_PATH=I:/Facebook_RF-BG/Facebook_RF/Facebook_RF/New folder

# Ollama host (used by backend service)
OLLAMA_HOST=http://ollama:11434
